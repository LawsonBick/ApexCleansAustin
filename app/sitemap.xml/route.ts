import {origin,services} from '@/lib/site';import {areas,articles} from '@/lib/content';
export function GET(){const routes=['/','/services','/about','/gallery','/reviews','/contact','/service-areas','/blog','/specials','/privacy',...services.map(s=>'/services/'+s.slug),...areas.map(a=>'/service-areas/'+a.slug),...articles.map(a=>'/blog/'+encodeURIComponent(a.slug))];return new Response('<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">'+routes.map(p=>`<url><loc>${origin+p}</loc></url>`).join('')+'</urlset>',{headers:{'Content-Type':'application/xml','X-Robots-Tag':'noindex'}});}
