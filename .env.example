# Preview stays non-indexable unless explicitly enabled for the production origin.
SITE_INDEXABLE=false
# Server-side only. Use a verified sender on an email domain you control.
RESEND_API_KEY=
QUOTE_FROM=
